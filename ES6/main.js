// Named exports
export function add(a, b) {
    return a + b;
}

export function subtract(a, b) {
    return a - b;
}

// Default export (only one default export allowed per file)
export default function multiply(a, b) {
    return a * b;
}
